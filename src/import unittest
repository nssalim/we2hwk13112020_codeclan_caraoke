import unittest
from src.guest import Guest
from src.drink import Drink
from src.food import Food

class TestGuest(unittest.TestCase):
    
    def setUp(self):
        
        self.drink = Drink("rum", 5.00, 30)
        self.food = Food("peanuts", 2.00, 25)
        self.cusguest_1 = guest("Jack", 26.00, 24, 0)
        self.guest_2 = guest("Jane", 50.00, 17, 10)
        
        
    def test_guest_name(self):
        self.assertEqual("Jack", self.guest.name)
        
        
    def test_guest_wallet(self):
        self.assertEqual(26.00, self.guest.wallet)

    
    def test_guest_age(self):
        self.assertEqual(24, self.customer.age)
        
    
    def test_guest_rendition_of_song(self):
        self.assertEqual(0, self.guest.rendition_of_song)
 
    
    def test_sufficient_funds__true_if_enough(self):
        self.assertEqual(True, self.guest.sufficient_funds(self.drink))
    
